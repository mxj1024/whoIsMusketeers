using UnityEngine;
using System.Collections;

namespace VikingCrewTools.Sidescroller {
    /// <summary>
    /// This script should be attached to all transforms that are to work as waypoints
    /// 
    /// These waypoints can then be attached to prefabs and will be moved to the 
    /// correct graph at start of game
    /// </summary>
    public class PointGraphWaypointScript : MonoBehaviour {


        // Use this for initialization
        void Start() {

        }

        // Update is called once per frame
        void Update() {

        }
    }
}
